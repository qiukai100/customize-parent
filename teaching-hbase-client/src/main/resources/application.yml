spring:
  application:
    name: teaching-hbase-client

server:
  port: 9000
  context-path: /teaching/hbase
  session:
    timeout: 60
  tomcat:
    uri-encoding: utf-8
    max-threads: 1000

eureka:
  client:
    service-url:
#      defaultZone: http://127.0.0.1:8761/eureka/
      defaultZone: http://eureka.didispace.com/eureka/